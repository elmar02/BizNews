@model List<Tag>

@{
    var count = 1;
}

<a href="/admin/tag/create" class="btn btn-warning">Create</a>
<div class="table-responsive">
    <table class="table table-dark table-bordered table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Tag Name</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tag in Model)
            {
                <tr>
                    <td>@(count++)</td>
                    <td>@tag.TagName</td>
                    <td>
                        <a href="/admin/tag/delete/@tag.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>